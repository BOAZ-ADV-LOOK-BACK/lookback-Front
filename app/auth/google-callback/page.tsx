'use client';

// 경로를 정확하게 지정해 주세요
// [KAN-46] 용 주석 (아무 수정 없이 커밋 한 번 더 만들기 위해)
import GoogleCallback from '../../../components/component/GoogleCallback';

const GoogleCallbackPage = () => {
  return <GoogleCallback />;
};

export default GoogleCallback;